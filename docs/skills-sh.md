# skills.sh

Install all skills with skills.sh:

```bash
npx skills add AlizHarb/agent-skills
```

Install a single skill:

```bash
npx skills add AlizHarb/agent-skills --skill create-action
```

The `skills/` directory is kept compatible with portable skill consumers.
