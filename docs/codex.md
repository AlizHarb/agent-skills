# Codex

Use the skills.sh-compatible skill folders:

```bash
npx skills add AlizHarb/agent-skills
```

Recommended presets:

- `core` for everyday Laravel work.
- `package` for package maintenance.
- `security` for security-sensitive repositories.
- `testing` for quality-focused sessions.
